name: flutter_chat_kit_ui
description: "A Flutter package that provides a drop-in chat UI widget compatible with OpenAI ChatKit backends"
version: 0.1.0
repository: https://github.com/meadworks/flutter_chatkit

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_markdown: ^0.7.0
  mime: ^2.0.0
  intl: ">=0.19.0 <1.0.0"
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
