name: flutter_chat_kit_ui
description: "Unofficial Flutter port of the OpenAI ChatKit UI. A drop-in chat widget compatible with OpenAI ChatKit backends."
version: 0.1.1
repository: https://github.com/meadworks/flutter_chatkit

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_markdown_plus: ^1.0.7
  mime: ^2.0.0
  intl: ">=0.19.0 <1.0.0"
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
